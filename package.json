{
  "name": "upm_version_increment_action",
  "version": "1.0.0",
  "description": "This action increments a Unity package version and tags the revision. Tagged URLs are added to the readme.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
